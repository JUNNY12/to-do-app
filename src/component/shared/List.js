import styled from "styled-components"


const ParentList = styled.ul`
    list-style-type:none;
    padding:;
    margin:0 1rem 0 1rem;
`

const List = styled.li`
    font-size:1rem;
    font-weight:bold;
`

export{ParentList, List}